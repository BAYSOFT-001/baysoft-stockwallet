// - Import Types
import { SET_APPLICATION_NAME } from './types';

// - Import Actions
import { SetApplicationName } from './actions';

const types = {
    SET_APPLICATION_NAME
};

const actions = {
    SetApplicationName
};

export {
    types, actions
};