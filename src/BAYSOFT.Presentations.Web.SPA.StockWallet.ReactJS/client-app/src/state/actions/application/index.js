// - Import Types
import {
    APPLICATION_NAME_SET,
    APPLICATION_MENU_OPEN,
    APPLICATION_MENU_CLOSE
} from './types';

// - Import Actions
import {
    SetApplicationName,
    ApplicationMenuOpen,
    ApplicationMenuClose
} from './actions';

const types = {
    APPLICATION_NAME_SET,
    APPLICATION_MENU_OPEN,
    APPLICATION_MENU_CLOSE
};

const actions = {
    SetApplicationName,
    ApplicationMenuOpen,
    ApplicationMenuClose
};

export {
    types, actions
};